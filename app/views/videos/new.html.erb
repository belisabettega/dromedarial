<%= simple_form_for(@video) do |f| %>
  <%= f.association :category, collection: @user.categories, prompt: 'Select category' %>
  <%= f.input :user_id, as: :hidden, input_html: { value: @user.id } %>
  <%= f.submit class: 'btn-default' %>
<% end %>