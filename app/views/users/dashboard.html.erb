<div class="container">
  <div class="row">
    <div class="col-12 d-flex">
      <div class="mr-5">
        <h3>My Categories</h3>
      </div>
      <div>
        <%= link_to "Add new category", new_category_path, class:'bttn-white' %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12 my-3 d-flex justify-content-between align-items-center">
      <div class="card border-top-card" style="width: 18rem;">
        <div class="card-body d-flex justify-content-center align-items-center">
          <div>
            <h5 class="card-title text-center">Your total until now</h5>
            <h5 class="card-title text-center headline-text">
              <% total_money = 0 %>
              <% current_user.videos.each do |video| %>
                <% if video.status == "completed" %>
                  <% total_money += video.category.price.to_f %>
                <%end%>
              <%end%>
              <i class="fas fa-dollar-sign"></i><%= total_money %>
            </h5>
          </div>
        </div>
      </div>
      <div class="card border-top-card" style="width: 18rem;">
        <div class="card-body d-flex justify-content-center align-items-center">
          <div>
            <h5 class="card-title text-center"> Completed videos </h5>
            <h5 class="card-title text-center headline-text">
            <% total_money = 0 %>
            <% current_user.videos.each do |video| %>
              <% if video.status == "completed" %>
              <% total_money += 1 %>
              <%end%>
            <%end%>
            <i class="fas fa-check"></i><%= total_money %>
            </h5>
          </div>
        </div>
      </div>

      <div class="card border-top-card" style="width: 18rem;">
        <div class="card-body d-flex justify-content-center align-items-center">
          <div>
            <h5 class="card-title text-center">Your rating media</h5>
            <h5 class="card-title text-center headline-text">
            <% total_money = 0 %>
            <% current_user.videos.each do |video| %>
              <% if video.status == "completed" %>
              <% total_money += video.rating %>
              <%end%>
            <%end%>
            <i class="far fa-star"></i><%= "#{total_money / current_user.videos.count}" %>
            </h5>
          </div>
        </div>
    </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="wrapper my-3">
        <% current_user.categories.each do |category| %>
          <div class="item d-flex justify-content-center align-items-center">
            <h6 class="text-card "><%= category.title %></h6>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="wrapper-video">
        <% @videos.each_with_index do |video, index| %>
          <%if video.status == "Pendent"%>
            <div class="item-video">
            <div class="text-card">
              <h3><%= video.category.title %></h3>
            </div>
            <p><%= video.user.name %></p>
            <% if policy(video).update? %>
              <%= link_to edit_video_path(video), class:"icons-cards" do %>
                <i class="fas fa-heart"></i>
              <%end%>
              <%= link_to change_status_path(video), class:"icons-cards" do %>
                <i class="fas fa-times"></i>
              <%end%>
            <% end %>
          </div>
        <%elsif index < 1%>
          <div class="item-video">
            <div class="text-card">
              <h3>No pending videos to send!</h3>
            </div>
          </div>
        <%end%>
      <% end %>
    </div>
  </div>
  </div>



    </div>
  </div>
</div>






<div class="container bg-primary text-white">
  <div class="row">
  <div class="col">





</div>


<div>



</div>

